import React from "react";
export interface postProps {creater:String, tags:String[], title:String, message:String,like:String}

const Post = ({creater, tags, title, message,like}:postProps) => {
  return (
        <div>
            
        </div>
    )
};

export default Post;
